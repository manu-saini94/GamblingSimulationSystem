public enum OddsType {
    FIXED,              // Fixed multiplier (e.g., 2x)
    PROBABILITY_BASED,  // Based on win probability
    AMERICAN,           // American odds format
    DECIMAL            // Decimal odds format
}
